import { Routes } from '@angular/router';
import { Mform } from './components/mform/mform';
import { Xform } from './components/xform/xform';

export const routes: Routes = [{ path: '', component: Xform }];
